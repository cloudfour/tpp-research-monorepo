import { Story, Preview, Meta, Props } from "@storybook/addon-docs/blocks";
import { html } from "lit-html";
import { text } from "@storybook/addon-knobs";

<Meta title="Button" decorators={[withKnobs]} />

# Buttons

<Preview>
  <Story name="Button">
    {() => {
      const slot = text("slot", "Button");
      return html`
        <c4-button>${slot}</c4-button>
      `;
    }}
  </Story>
</Preview>
